{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "pricing-service.fullname" . }}-netpol
  labels:
    {{- include "pricing-service.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "pricing-service.selectorLabels" . | nindent 6 }}
  
  policyTypes:
    - Ingress
    - Egress
  
  ingress:
    # Allow from API Gateway (core namespace)
    - from:
        - namespaceSelector:
            matchLabels:
              type: core
        - namespaceSelector:
            matchLabels:
              type: domain
      ports:
        - port: {{ .Values.service.targetPort }}
          protocol: TCP
    
    # Allow metrics scraping from Prometheus
    - from:
        - namespaceSelector:
            matchLabels:
              name: observability
      ports:
        - port: {{ .Values.observability.metrics.port }}
          protocol: TCP
  
  egress:
    # Allow DNS
    - to:
        - namespaceSelector: {}
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    
    # Allow to Security-Config-Service (core namespace)
    - to:
        - namespaceSelector:
            matchLabels:
              type: core
      ports:
        - port: 3005
          protocol: TCP
    
    # Allow to Kafka (data namespace)
    - to:
        - namespaceSelector:
            matchLabels:
              type: data
      ports:
        - port: 9092
          protocol: TCP
    
    # Allow to Redis (data namespace)
    - to:
        - namespaceSelector:
            matchLabels:
              type: data
      ports:
        - port: 6379
          protocol: TCP
    
    # Allow to Tempo for tracing
    - to:
        - namespaceSelector:
            matchLabels:
              name: observability
      ports:
        - port: 4318
          protocol: TCP
{{- end }}
