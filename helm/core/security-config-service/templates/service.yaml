apiVersion: v1
kind: Service
metadata:
  name: {{ include "security-config-service.fullname" . }}
  labels:
    {{- include "security-config-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.observability.metrics.enabled }}
    - port: {{ .Values.observability.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- end }}
  selector:
    {{- include "security-config-service.selectorLabels" . | nindent 4 }}
